{
  "name": "sns-kang88",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check core.js && node --check store.js && node --check seed.js && node --check photos.js && node --check photo-ui.js && node --check cloud-config.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs"
  }
}
